---
title: Component Name
description: Brief description of what this component does.
---

# Component Name

A brief introduction to the component and its purpose.

## Installation

First, install the required dependencies:

```bash
npm install @radix-ui/react-[primitive] lucide-react
```

Then copy and paste the component code:

<ComponentPreview name="component-name" />

## Usage

```tsx
import { ComponentName } from "@/components/ui/component-name"

export function Example() {
  return (
    <ComponentName>
      Content goes here
    </ComponentName>
  )
}
```

## Examples

### Basic Example

<ComponentPreview name="component-name" />

### With Variants

<ComponentPreview name="component-name-variant" />

## API Reference

### ComponentName

| Prop | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
| `variant` | `"default" \| "secondary"` | `"default"` | The visual style variant |
| `size` | `"sm" \| "md" \| "lg"` | `"md"` | The size of the component |
| `disabled` | `boolean` | `false` | Whether the component is disabled |

### ComponentNameContent

| Prop | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
| `children` | `ReactNode` | - | The content to display |

## Accessibility

This component follows WAI-ARIA guidelines:

- Uses semantic HTML elements
- Provides proper keyboard navigation
- Includes appropriate ARIA attributes
- Supports screen readers

## Customization

You can customize the component by modifying the CSS classes or extending the variant styles:

```tsx
// Custom variant example
const customVariants = {
  variant: {
    default: "bg-primary text-primary-foreground",
    custom: "bg-gradient-to-r from-blue-500 to-purple-600 text-white",
  }
}
```